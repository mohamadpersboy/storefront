export default function LoginPlaceholderPage() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="w-full max-w-sm rounded-[var(--radius-lg)] border border-border bg-surface p-6 text-center">
        <h1 className="text-lg font-semibold text-foreground">ورود</h1>
        <p className="mt-2 text-sm text-muted">
          صفحه ورود با شماره موبایل و کد یک‌بارمصرف در فاز بعدی
          (Authentication Feature) پیاده‌سازی می‌شود.
        </p>
      </div>
    </main>
  );
}
